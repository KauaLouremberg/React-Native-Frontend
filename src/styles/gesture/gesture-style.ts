import { StyleSheet } from 'react-native';
import { colors } from '../../core/constants/colors';

const { white } = colors;

export const gestureStyle = StyleSheet.create({
  gesture: {
    flex: 1,
    backgroundColor: white,
  },
});
